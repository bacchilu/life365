export const CurrencyFormatter = new Intl.NumberFormat('it-IT', {
    style: 'currency',
    currency: 'EUR',
});

export const PercentFormatter = new Intl.NumberFormat('it-IT', {
    style: 'percent',
});
